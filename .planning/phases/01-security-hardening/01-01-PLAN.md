---
phase: 01-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/__tests__/integration/security.test.ts
  - client/src/__tests__/helpers.ts
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, SEC-06, SEC-07, SEC-08, SEC-09, SEC-10]

must_haves:
  truths:
    - "security.test.ts file exists and covers SEC-01 through SEC-08 and SEC-10 with failing/skipped stubs"
    - "helpers.ts is updated to remove recovery_keys and transfer_codes from ALL_TABLES (in preparation for them going private)"
    - "Running `cd client && npm run test:integration` executes the security suite without crashing the runner (tests may fail with 'not implemented' but the file parses and runs)"
  artifacts:
    - path: "client/src/__tests__/integration/security.test.ts"
      provides: "Integration test stubs for all SEC-01 through SEC-10 behaviors"
    - path: "client/src/__tests__/helpers.ts"
      provides: "Updated ALL_TABLES list without private tables"
  key_links:
    - from: "client/src/__tests__/integration/security.test.ts"
      to: "client/src/__tests__/helpers.ts"
      via: "imports connect, waitFor, disconnect"
      pattern: "import.*helpers"
---

<objective>
Create the Wave 0 test scaffold for the security phase. Every requirement in SEC-01 through SEC-10 gets a test case (integration tests for SEC-01 through SEC-08 and SEC-10; manual verification note for SEC-09). Tests are written first — they will fail until the server changes in later plans are deployed.

Purpose: Nyquist compliance — every subsequent plan has automated verification ready before implementation begins.
Output: security.test.ts with failing test stubs + helpers.ts updated to not subscribe to tables that will become private.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-hardening/01-RESEARCH.md
@.planning/phases/01-security-hardening/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update helpers.ts — remove private tables from ALL_TABLES</name>
  <files>client/src/__tests__/helpers.ts</files>
  <behavior>
    - ALL_TABLES no longer includes 'SELECT * FROM recovery_keys'
    - ALL_TABLES no longer includes 'SELECT * FROM transfer_codes'
    - ALL other table subscriptions remain unchanged
    - connect() function signature and behavior unchanged
    - waitFor() and disconnect() unchanged
  </behavior>
  <action>
    Edit `client/src/__tests__/helpers.ts`. Remove the two entries for `recovery_keys` and `transfer_codes` from the `ALL_TABLES` array (lines 13-14 in current file). These tables will become private in Plan 03; subscribing to private tables via ALL_TABLES will break all tests once the schema change ships.

    Do NOT change the connect(), waitFor(), or disconnect() functions. Do NOT add any new tables at this stage.
  </action>
  <verify>
    <automated>cd /Users/lbi/Projects/spacetimemath/client && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>helpers.ts has 9 table entries in ALL_TABLES (all public tables except recovery_keys and transfer_codes); existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create security.test.ts integration test scaffold</name>
  <files>client/src/__tests__/integration/security.test.ts</files>
  <behavior>
    - SEC-01: A second client identity gets 0 rows from recovery_keys after tables are private
    - SEC-02: A second client identity gets 0 rows from transfer_codes after tables are private
    - SEC-03: Calling getMyRecoveryCode reducer populates recovery_code_results for caller's identity
    - SEC-04: 81st submit_answer call is rejected with an error
    - SEC-05: submit_answer with responseMs=100 is rejected with an error
    - SEC-06: submit_answer with a Tier 2 problem pair for a Tier 0 player is rejected
    - SEC-07: use_transfer_code with a non-existent code string returns an error
    - SEC-08: register with a username containing a null byte (\0) is rejected with an error
    - SEC-10: submit_answer without a valid problem_token (or with wrong token) is rejected
    - SEC-09: marked as manual-only with a comment explaining why (scheduler timing not testable in fast suite)
  </behavior>
  <action>
    Create `client/src/__tests__/integration/security.test.ts`.

    Follow the exact pattern of `client/src/__tests__/integration/register.test.ts`: use `describe`, `beforeAll`, `afterAll`, `it`. Import from `../helpers.js`.

    For each test case, write the test as if the server implementation already exists:
    - For table visibility tests (SEC-01, SEC-02): connect as clientA, register, then connect as clientB with a fresh identity (no token), and assert that `clientB.conn.db.recovery_keys.iter()` has 0 rows. Write this test now — it will pass once Plan 03 ships the table-private change.
    - For reducer tests (SEC-03): call `client.conn.reducers.getMyRecoveryCode({})`, then use `waitFor` to poll `client.conn.db.recoveryCodeResults.iter()` for a matching row.
    - For submit_answer rejection tests (SEC-04, SEC-05, SEC-06): these require an active session. Create a beforeAll that registers a player and starts a session. For each test: wrap the reducer call in a try/catch and assert it throws, OR use the reducers error callback pattern from the existing solo_sprint.test.ts — check how errors are surfaced in existing tests first.
    - For SEC-07: call `client.conn.reducers.useTransferCode({ code: 'XXXXXX' })` and assert it throws.
    - For SEC-08: call `client.conn.reducers.register({ username: 'bad\0name' })` and assert it throws.
    - For SEC-10: wrap in try/catch, assert `submitAnswer({ ..., problemToken: 'INVALID' })` throws.

    Mark SEC-09 with a `it.skip('SEC-09: transfer code TTL expiry — manual verification only', ...)` with a comment: "Scheduled reducer fires after 10 minutes. Verify manually: create code, wait 10+ min, confirm row absent in SpacetimeDB dashboard."

    Use unique usernames per describe block to avoid cross-test interference (prefix with 'sec_' + requirement ID).

    The tests will fail until server Plans 02-05 are deployed. That is expected. The goal is that the test FILE parses and runs without crashing the test runner.
  </action>
  <verify>
    <automated>cd /Users/lbi/Projects/spacetimemath/client && npm run test:integration -- --reporter=verbose security.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>security.test.ts exists, all tests in the file run (pass, fail, or skip — no parse errors), SEC-09 is skipped with a clear comment, all other SEC-01 through SEC-10 test cases are present.</done>
</task>

</tasks>

<verification>
Run `cd /Users/lbi/Projects/spacetimemath/client && npm run test:integration -- --reporter=verbose security.test.ts` and confirm:
1. No TypeScript compile errors or import errors
2. ALL_TABLES in helpers.ts has 9 entries (recovery_keys and transfer_codes absent)
3. SEC-09 test is skipped
4. SEC-01 through SEC-08 and SEC-10 test cases are present (they may fail — that's expected)
</verification>

<success_criteria>
Test scaffold in place. All subsequent plans can reference `npm run test:integration -- --reporter=verbose security.test.ts` as their automated verify command. Existing unit tests (`npm test`) still pass green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-hardening/01-01-SUMMARY.md`
</output>
