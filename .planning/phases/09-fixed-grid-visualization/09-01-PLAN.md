---
phase: 09-fixed-grid-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/DotArray.tsx
autonomous: false
requirements:
  - VIZ-01
  - VIZ-02

must_haves:
  truths:
    - "For any a×b problem, exactly 100 dot cells are rendered (10 rows × 10 columns)"
    - "The top-left a×b rectangle is visually highlighted (accent color, full opacity)"
    - "Cells outside the a×b rectangle are visually dimmed (neutral color, low opacity)"
    - "Grid dimensions are identical for every problem — no layout shift when advancing"
    - "The faded prop still fades the whole component to 20% opacity"
  artifacts:
    - path: "client/src/components/DotArray.tsx"
      provides: "Fixed 10×10 dot grid component"
      contains: "GRID_SIZE"
  key_links:
    - from: "client/src/components/DotArray.tsx"
      to: "client/src/pages/SprintPage.tsx"
      via: "props a, b, faded — interface unchanged"
      pattern: "DotArray a=\\{problem\\.a\\}"
---

<objective>
Rewrite DotArray.tsx to always render a fixed 10×10 grid. The top-left a×b rectangle is highlighted with accent color; all remaining cells are dimmed with a neutral color. The grid never resizes between problems.

Purpose: Players see a stable, predictable visual for every multiplication problem — no layout shift as they advance.
Output: Updated DotArray.tsx, verified by human smoke test in SprintPage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fixed-grid-visualization/09-RESEARCH.md
@.planning/phases/09-fixed-grid-visualization/09-VALIDATION.md

<interfaces>
<!-- Current DotArray.tsx — prop interface must remain identical -->
```tsx
interface Props {
  a: number;
  b: number;
  faded?: boolean;
}
```

<!-- Only call site — SprintPage.tsx lines 491-499 -->
```tsx
<div className="row-center mb-2">
  <DotArray a={problem.a} b={problem.b} faded={mastery !== 'untouched'} />
</div>
```

<!-- Design tokens available in index.css -->
var(--accent)  — yellow #FBBA00, used for highlighted cells
var(--card2)   — neutral surface color, used for dimmed cells
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DotArray.tsx to fixed 10×10 grid</name>
  <files>client/src/components/DotArray.tsx</files>
  <action>
Replace the entire file contents with the fixed-grid implementation below. The key change is: both render loops now iterate INDICES (0-9) regardless of a and b. The highlight condition `row < a && col < b` determines whether each cell is accent-colored or dimmed.

Constants to define at module level:
- `const GRID_SIZE = 10` — total grid dimension
- `const BLOCK_SIZE = 5` — cells per block (for block-gap logic)
- `const CELL_SIZE = 12` — dot size in px
- `const CELL_GAP = 3` — gap between dots within a block
- `const BLOCK_GAP = 8` — gap between the two 5-cell blocks

Generate INDICES once: `const INDICES = Array.from({ length: GRID_SIZE }, (_, i) => i)`

Outer container: keep `className="mb-3"`, `display: 'inline-block'`, `opacity: faded ? 0.2 : 1`, `transition: 'opacity 1s'`.

Render each row as two sub-groups of 5 cells side-by-side:
- Outer row wrapper: `display: 'flex'`, `gap: BLOCK_GAP`, `marginBottom: row === BLOCK_SIZE - 1 ? BLOCK_GAP : CELL_GAP`
- Each sub-group (columns 0-4 and columns 5-9): `display: 'flex'`, `gap: CELL_GAP`
- Each cell: `width: CELL_SIZE`, `height: CELL_SIZE`, `borderRadius: 2`
  - Highlighted (row < a && col < b): `background: 'var(--accent)'`, `opacity: 1`
  - Dimmed (otherwise): `background: 'var(--card2)'`, `opacity: 0.35`

Use `clampToGrid` safety clamp for a and b: `const clampToGrid = (n: number) => Math.max(1, Math.min(GRID_SIZE, n))` — then `const rows = clampToGrid(a); const cols = clampToGrid(b)` — use rows/cols in the highlight condition.

Update the JSDoc comment to reflect the new behavior: "Always renders a 10×10 grid. The top-left a×b rectangle is highlighted; remaining cells are dimmed."

Do NOT change the prop interface (`a`, `b`, `faded`). Do NOT add any new props. Do NOT touch SprintPage.tsx.
  </action>
  <verify>
    <automated>cd /Users/lbi/Projects/spacetimemath/client && npm test</automated>
  </verify>
  <done>npm test passes (pre-commit hook green). DotArray.tsx iterates INDICES for both loops, not `a` or `b` for length.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed 10×10 DotArray component in SprintPage. For every tier-0 problem, the dot grid now shows all 100 cells — the top-left a×b rectangle highlighted in yellow, the rest dimmed in neutral color. Grid size is constant regardless of problem.</what-built>
  <how-to-verify>
1. Run the dev server: `cd /Users/lbi/Projects/spacetimemath/client && npm run dev`
2. Open the app in browser and navigate to SprintPage (start a sprint)
3. Confirm for a small problem (e.g. 3×4): dot grid shows 10 columns and 10 rows. Top-left 3 rows × 4 columns are bright yellow. Remaining 88 cells are dimmed/neutral.
4. Advance through several problems. Confirm the grid dimensions do not shift — the bounding box stays the same size every problem.
5. Confirm a large problem (e.g. 9×8): top-left 9 rows × 8 columns are highlighted; 28 cells are dimmed.
6. Confirm that when a problem has mastery "struggling" (not first attempt), the entire DotArray fades to ~20% opacity.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes (lint + Vitest unit suite unaffected by visual component change)
- Human smoke test confirms: fixed 10×10 grid, correct rectangle highlight, no layout shift across problems
- VIZ-01: grid always exactly 10×10 ✓
- VIZ-02: top-left a×b highlighted, rest dimmed ✓
</verification>

<success_criteria>
- DotArray always renders 100 cells (10×10) regardless of problem values
- Top-left a×b rectangle is visually distinct (accent color, full opacity)
- Remaining cells are visually neutral (card2 color, 0.35 opacity)
- No layout shift as player advances through problems
- faded prop still works (whole component at 0.2 opacity)
- npm test green, pre-commit hook passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-fixed-grid-visualization/09-01-SUMMARY.md`
</output>
