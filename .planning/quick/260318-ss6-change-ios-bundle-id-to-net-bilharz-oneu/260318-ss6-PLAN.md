---
phase: quick-260318-ss6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/ios/App/App.xcodeproj/project.pbxproj
autonomous: true
requirements: []
must_haves:
  truths:
    - "Both Debug and Release configurations have PRODUCT_BUNDLE_IDENTIFIER = net.bilharz.oneup"
  artifacts:
    - path: "client/ios/App/App.xcodeproj/project.pbxproj"
      provides: "Xcode project with updated bundle identifier"
      contains: "net.bilharz.oneup"
  key_links:
    - from: "client/ios/App/App.xcodeproj/project.pbxproj"
      to: "App Store Connect"
      via: "PRODUCT_BUNDLE_IDENTIFIER used by Xcode during archive"
      pattern: "PRODUCT_BUNDLE_IDENTIFIER = net\\.bilharz\\.oneup"
---

<objective>
Change the iOS bundle identifier from com.bettermarks.oneup to net.bilharz.oneup in the Xcode project file, for both Debug and Release build configurations.

Purpose: The app must be submitted under the net.bilharz.oneup bundle ID (the correct App Store identity for this developer account).
Output: Updated project.pbxproj with net.bilharz.oneup in both configuration blocks.
</objective>

<execution_context>
@/Users/lbi/Projects/spacetimemath/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lbi/Projects/spacetimemath/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PRODUCT_BUNDLE_IDENTIFIER in project.pbxproj</name>
  <files>client/ios/App/App.xcodeproj/project.pbxproj</files>
  <action>
    In client/ios/App/App.xcodeproj/project.pbxproj, replace every occurrence of:
      PRODUCT_BUNDLE_IDENTIFIER = com.bettermarks.oneup;
    with:
      PRODUCT_BUNDLE_IDENTIFIER = net.bilharz.oneup;

    There are exactly two occurrences — one in the Debug XCBuildConfiguration block and one in the Release XCBuildConfiguration block (lines ~309 and ~331). Replace both. Do not touch any other lines.

    Use the Edit tool for each occurrence, or Read the full file and Write it back with both replacements applied — do not use sed via Bash (the Write tool is preferred for correctness).

    After editing, confirm with grep that no occurrence of com.bettermarks.oneup remains in the file.
  </action>
  <verify>
    <automated>grep -c "net.bilharz.oneup" /Users/lbi/Projects/spacetimemath/client/ios/App/App.xcodeproj/project.pbxproj && ! grep -q "com.bettermarks.oneup" /Users/lbi/Projects/spacetimemath/client/ios/App/App.xcodeproj/project.pbxproj && echo "OK"</automated>
  </verify>
  <done>grep shows exactly 2 occurrences of net.bilharz.oneup, zero occurrences of com.bettermarks.oneup</done>
</task>

</tasks>

<verification>
grep -c "net.bilharz.oneup" client/ios/App/App.xcodeproj/project.pbxproj   # must return 2
grep "com.bettermarks.oneup" client/ios/App/App.xcodeproj/project.pbxproj  # must return nothing
</verification>

<success_criteria>
project.pbxproj has PRODUCT_BUNDLE_IDENTIFIER = net.bilharz.oneup in both Debug and Release configurations. No trace of com.bettermarks.oneup remains. Ready for Xcode archive and App Store Connect upload.
</success_criteria>

<output>
After completion, create .planning/quick/260318-ss6-change-ios-bundle-id-to-net-bilharz-oneu/260318-ss6-SUMMARY.md
</output>
